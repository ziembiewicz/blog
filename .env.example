# Copy to .env and paste your key. .env is git-ignored - never commit it.
#
# Only the Access Key is needed. The Secret Key is used exclusively for OAuth
# (acting on behalf of a logged-in Unsplash user) and this project never does that.
UNSPLASH_ACCESS_KEY=your-access-key-here
